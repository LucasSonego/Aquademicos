import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;

  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .warning {
    color: #999;
    font-weight: 500;
    font-family: inherit;
  }
`;

export const Page = styled.div``;
