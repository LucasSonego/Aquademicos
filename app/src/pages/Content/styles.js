import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;

  .school-class {
    color: #777;
    font-family: inherit;
    margin: 0;
  }

  .no-content {
    margin-top: 20px;
    background: #eee;
    border-radius: 10px;
    padding: 15px 20px;

    p {
      font-family: inherit;
      font-size: 18px;
      color: #333;
      margin: 0;
    }

    span {
      color: #777;
    }
  }
`;

export const Page = styled.div``;
